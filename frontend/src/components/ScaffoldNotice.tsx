/** Presentational stub only. HITL screens are out of scope for ST-006. */
export function ScaffoldNotice() {
  return (
    <>
      <p>Frontend scaffold (ST-006). Vite + React + TypeScript.</p>
      <p>
        HITL screens live in later tickets. Folders <code>src/pages</code>,{' '}
        <code>src/components</code>, <code>src/api</code> are reserved stubs.
      </p>
    </>
  )
}

CI_DATABASE_URL=postgresql+psycopg://ci:ci@localhost:5432/ci
CI_ARTIFACT_ROOT=../data/artifacts
# Replace with generated random tokens; identities and roles are server-owned.
CI_ACCOUNTS={"replace-with-random-token":{"id":"local-admin","role":"admin"}}

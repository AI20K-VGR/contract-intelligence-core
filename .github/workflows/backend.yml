# CI for backend/. The team fills in the steps once the stack is chosen (install, lint, test, build).
# Runs only when backend/ changes. Ask the mentor to make it a required check when it is stable.
name: backend

on:
  pull_request:
    paths: ["backend/**"]
  push:
    branches: [develop, main]
    paths: ["backend/**"]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - name: TODO (team) — install, lint, test
        run: echo "no CI defined for backend yet"

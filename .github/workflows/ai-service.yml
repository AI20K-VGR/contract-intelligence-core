# CI for ai-service/. The team fills in the steps once the stack is chosen (install, lint, test, build).
# Runs only when ai-service/ changes. Ask the mentor to make it a required check when it is stable.
name: ai-service

on:
  pull_request:
    paths: ["ai-service/**"]
  push:
    branches: [develop, main]
    paths: ["ai-service/**"]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-service
    steps:
      - uses: actions/checkout@v4
      - name: TODO (team) — install, lint, test
        run: echo "no CI defined for ai-service yet"
